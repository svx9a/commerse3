<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thai Affiliate Empire - Complete Automation System</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        :root {
            --primary: #8b5cf6;
            --primary-dark: #7c3aed;
            --secondary: #ec4899;
            --dark: #0f172a;
            --darker: #020617;
            --light: #f8fafc;
            --gray: #64748b;
            --success: #10b981;
            --warning: #f59e0b;
            --danger: #ef4444;
        }

        body {
            background: linear-gradient(135deg, var(--darker) 0%, var(--dark) 50%, #1e1b4b 100%);
            color: var(--light);
            min-height: 100vh;
            line-height: 1.6;
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Header */
        .header {
            background: rgba(15, 23, 42, 0.8);
            backdrop-filter: blur(20px);
            border-bottom: 1px solid rgba(139, 92, 246, 0.2);
            position: sticky;
            top: 0;
            z-index: 100;
        }

        .header-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 15px 0;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .logo-icon {
            width: 45px;
            height: 45px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .logo-text {
            font-size: 24px;
            font-weight: 900;
            background: linear-gradient(to right, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
        }

        .logo-subtitle {
            font-size: 12px;
            color: var(--primary);
        }

        .header-controls {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .status-badge {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 8px 16px;
            background: rgba(16, 185, 129, 0.2);
            border: 1px solid rgba(16, 185, 129, 0.3);
            border-radius: 10px;
            font-weight: 700;
            font-size: 14px;
            color: #10b981;
        }

        .status-dot {
            width: 8px;
            height: 8px;
            background: #10b981;
            border-radius: 50%;
            animation: pulse 2s infinite;
        }

        .auto-mode-btn {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 10px 20px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border: none;
            border-radius: 10px;
            font-weight: 700;
            color: white;
            cursor: pointer;
            transition: all 0.3s;
            box-shadow: 0 4px 15px rgba(139, 92, 246, 0.4);
        }

        .auto-mode-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(139, 92, 246, 0.6);
        }

        /* Navigation */
        .nav {
            background: rgba(2, 6, 23, 0.5);
            backdrop-filter: blur(10px);
            border-bottom: 1px solid rgba(139, 92, 246, 0.2);
            position: sticky;
            top: 75px;
            z-index: 90;
        }

        .nav-content {
            display: flex;
            gap: 5px;
            padding: 10px 0;
            overflow-x: auto;
        }

        .nav-tab {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 12px 20px;
            background: transparent;
            border: none;
            border-radius: 10px;
            font-weight: 700;
            color: var(--gray);
            cursor: pointer;
            transition: all 0.3s;
            white-space: nowrap;
        }

        .nav-tab.active {
            background: rgba(139, 92, 246, 0.1);
            color: var(--primary);
            border-bottom: 2px solid var(--primary);
        }

        .nav-tab:hover:not(.active) {
            background: rgba(139, 92, 246, 0.05);
            color: var(--light);
        }

        /* Main Content */
        .main-content {
            padding: 30px 0 80px;
        }

        .tab-content {
            display: none;
        }

        .tab-content.active {
            display: block;
        }

        /* Dashboard */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: linear-gradient(135deg, rgba(30, 41, 59, 0.7), rgba(15, 23, 42, 0.9));
            border: 1px solid rgba(139, 92, 246, 0.2);
            border-radius: 16px;
            padding: 25px;
            transition: all 0.3s;
        }

        .stat-card:hover {
            border-color: rgba(139, 92, 246, 0.4);
            transform: translateY(-5px);
        }

        .stat-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 20px;
        }

        .stat-label {
            font-size: 14px;
            font-weight: 700;
            color: var(--gray);
        }

        .stat-icon {
            width: 45px;
            height: 45px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
        }

        .icon-today {
            background: linear-gradient(135deg, #f59e0b, #f97316);
        }

        .icon-week {
            background: linear-gradient(135deg, var(--primary), var(--secondary));
        }

        .icon-month {
            background: linear-gradient(135deg, #10b981, #34d399);
        }

        .stat-value {
            font-size: 32px;
            font-weight: 900;
            margin-bottom: 10px;
        }

        .stat-trend {
            display: flex;
            align-items: center;
            gap: 5px;
            font-size: 14px;
            font-weight: 700;
            color: var(--success);
        }

        .mini-stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-bottom: 30px;
        }

        .mini-stat {
            background: rgba(15, 23, 42, 0.7);
            border: 1px solid rgba(139, 92, 246, 0.1);
            border-radius: 12px;
            padding: 20px;
        }

        .mini-stat-header {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 10px;
        }

        .mini-stat-icon {
            color: var(--primary);
            font-size: 16px;
        }

        .mini-stat-label {
            font-size: 12px;
            font-weight: 700;
            color: var(--gray);
        }

        .mini-stat-value {
            font-size: 22px;
            font-weight: 900;
        }

        .auto-pilot-card {
            background: linear-gradient(135deg, rgba(139, 92, 246, 0.1), rgba(236, 72, 153, 0.1));
            border: 1px solid rgba(139, 92, 246, 0.3);
            border-radius: 16px;
            padding: 25px;
            margin-bottom: 30px;
        }

        .auto-pilot-header {
            display: flex;
            align-items: center;
            gap: 10px;
            margin-bottom: 20px;
        }

        .auto-pilot-dot {
            width: 10px;
            height: 10px;
            background: var(--primary);
            border-radius: 50%;
            animation: pulse 2s infinite;
        }

        .auto-pilot-title {
            font-size: 20px;
            font-weight: 900;
            color: var(--primary);
        }

        .auto-pilot-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
        }

        .auto-pilot-item {
            background: rgba(0, 0, 0, 0.3);
            border-radius: 12px;
            padding: 20px;
        }

        .auto-pilot-item-header {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 10px;
        }

        .auto-pilot-item-icon {
            color: var(--primary);
            font-size: 16px;
        }

        .auto-pilot-item-label {
            font-size: 14px;
            color: var(--gray);
        }

        .auto-pilot-item-value {
            font-size: 18px;
            font-weight: 700;
        }

        /* Products Tab */
        .scanner-card {
            background: linear-gradient(135deg, rgba(30, 41, 59, 0.7), rgba(15, 23, 42, 0.9));
            border: 1px solid rgba(139, 92, 246, 0.2);
            border-radius: 16px;
            padding: 25px;
            margin-bottom: 30px;
        }

        .scanner-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 20px;
        }

        .scanner-title {
            font-size: 24px;
            font-weight: 900;
        }

        .scanner-description {
            color: var(--gray);
            margin-bottom: 20px;
        }

        .scan-btn {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 12px 24px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border: none;
            border-radius: 10px;
            font-weight: 700;
            color: white;
            cursor: pointer;
            transition: all 0.3s;
        }

        .scan-btn:disabled {
            opacity: 0.7;
            cursor: not-allowed;
        }

        .scan-btn:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(139, 92, 246, 0.6);
        }

        .progress-container {
            margin-top: 20px;
        }

        .progress-header {
            display: flex;
            justify-content: space-between;
            font-size: 14px;
            color: var(--gray);
            margin-bottom: 8px;
        }

        .progress-bar {
            height: 8px;
            background: rgba(139, 92, 246, 0.2);
            border-radius: 4px;
            overflow: hidden;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(to right, var(--primary), var(--secondary));
            border-radius: 4px;
            transition: width 0.3s;
        }

        .products-table {
            background: rgba(15, 23, 42, 0.7);
            border: 1px solid rgba(139, 92, 246, 0.2);
            border-radius: 16px;
            overflow: hidden;
        }

        .table-header {
            background: rgba(0, 0, 0, 0.3);
            padding: 20px;
            border-bottom: 1px solid rgba(139, 92, 246, 0.2);
        }

        .table-title {
            font-size: 20px;
            font-weight: 900;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        th {
            text-align: left;
            padding: 15px 20px;
            font-weight: 700;
            font-size: 12px;
            color: var(--primary);
            text-transform: uppercase;
            background: rgba(0, 0, 0, 0.2);
            border-bottom: 1px solid rgba(139, 92, 246, 0.2);
        }

        td {
            padding: 15px 20px;
            border-bottom: 1px solid rgba(139, 92, 246, 0.1);
        }

        tr:last-child td {
            border-bottom: none;
        }

        tr:hover {
            background: rgba(139, 92, 246, 0.05);
        }

        .platform-badge {
            padding: 5px 12px;
            background: rgba(139, 92, 246, 0.2);
            color: var(--primary);
            border-radius: 20px;
            font-size: 12px;
            font-weight: 700;
        }

        .trend-bar {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .bar-container {
            flex: 1;
            height: 6px;
            background: rgba(139, 92, 246, 0.2);
            border-radius: 3px;
            overflow: hidden;
        }

        .bar-fill {
            height: 100%;
            background: linear-gradient(to right, #10b981, #34d399);
            border-radius: 3px;
        }

        .generate-btn {
            display: flex;
            align-items: center;
            gap: 6px;
            padding: 8px 16px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border: none;
            border-radius: 8px;
            font-size: 12px;
            font-weight: 700;
            color: white;
            cursor: pointer;
            transition: all 0.3s;
        }

        .generate-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(139, 92, 246, 0.5);
        }

        /* Affiliates Tab */
        .affiliates-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 20px;
        }

        .affiliate-card {
            background: linear-gradient(135deg, rgba(30, 41, 59, 0.7), rgba(15, 23, 42, 0.9));
            border: 1px solid rgba(139, 92, 246, 0.2);
            border-radius: 16px;
            padding: 25px;
            transition: all 0.3s;
        }

        .affiliate-card:hover {
            border-color: rgba(139, 92, 246, 0.4);
            transform: translateY(-5px);
        }

        .affiliate-header {
            display: flex;
            align-items: flex-start;
            justify-content: space-between;
            margin-bottom: 20px;
        }

        .affiliate-name {
            font-size: 20px;
            font-weight: 900;
            margin-bottom: 8px;
        }

        .affiliate-meta {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .affiliate-platform {
            padding: 4px 10px;
            background: rgba(139, 92, 246, 0.2);
            color: var(--primary);
            border-radius: 12px;
            font-size: 12px;
            font-weight: 700;
        }

        .affiliate-followers {
            font-size: 14px;
            color: var(--gray);
        }

        .elite-badge {
            padding: 6px 12px;
            background: linear-gradient(135deg, #f59e0b, #f97316);
            border-radius: 12px;
            font-size: 12px;
            font-weight: 900;
            color: white;
        }

        .affiliate-stats {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
            margin-bottom: 20px;
        }

        .affiliate-stat {
            background: rgba(0, 0, 0, 0.3);
            border-radius: 12px;
            padding: 15px;
        }

        .affiliate-stat-label {
            font-size: 12px;
            color: var(--gray);
            margin-bottom: 5px;
        }

        .affiliate-stat-value {
            font-size: 20px;
            font-weight: 900;
        }

        .conversion-value {
            color: var(--success);
        }

        .affiliate-action {
            width: 100%;
            padding: 12px;
            background: rgba(139, 92, 246, 0.1);
            border: 1px solid rgba(139, 92, 246, 0.3);
            border-radius: 10px;
            color: var(--primary);
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s;
        }

        .affiliate-action:hover {
            background: rgba(139, 92, 246, 0.2);
        }

        /* Campaigns Tab */
        .campaigns-list {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .campaign-card {
            background: linear-gradient(135deg, rgba(30, 41, 59, 0.7), rgba(15, 23, 42, 0.9));
            border: 1px solid rgba(139, 92, 246, 0.2);
            border-radius: 16px;
            padding: 25px;
        }

        .campaign-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 20px;
        }

        .campaign-info h3 {
            font-size: 20px;
            font-weight: 900;
            margin-bottom: 8px;
        }

        .campaign-channel {
            padding: 4px 12px;
            background: rgba(59, 130, 246, 0.2);
            color: #3b82f6;
            border-radius: 12px;
            font-size: 12px;
            font-weight: 700;
        }

        .winning-badge {
            display: flex;
            align-items: center;
            gap: 6px;
            padding: 8px 16px;
            background: linear-gradient(135deg, #10b981, #34d399);
            border-radius: 12px;
            font-size: 14px;
            font-weight: 900;
            color: white;
        }

        .campaign-stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 15px;
        }

        .campaign-stat {
            background: rgba(0, 0, 0, 0.3);
            border-radius: 12px;
            padding: 20px;
        }

        .campaign-stat-label {
            font-size: 12px;
            color: var(--gray);
            margin-bottom: 8px;
        }

        .campaign-stat-value {
            font-size: 24px;
            font-weight: 900;
        }

        .spend-value {
            color: white;
        }

        .revenue-value {
            color: var(--success);
        }

        .roi-value {
            color: var(--primary);
        }

        /* Content Tab */
        .content-generator {
            background: linear-gradient(135deg, rgba(30, 41, 59, 0.7), rgba(15, 23, 42, 0.9));
            border: 1px solid rgba(139, 92, 246, 0.2);
            border-radius: 16px;
            padding: 40px;
            text-align: center;
        }

        .content-icon {
            width: 80px;
            height: 80px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 25px;
            font-size: 32px;
        }

        .content-title {
            font-size: 32px;
            font-weight: 900;
            margin-bottom: 15px;
        }

        .content-description {
            color: var(--gray);
            max-width: 600px;
            margin: 0 auto 30px;
            line-height: 1.6;
        }

        .platforms-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 15px;
            margin-bottom: 30px;
            max-width: 700px;
            margin-left: auto;
            margin-right: auto;
        }

        .platform-item {
            background: rgba(0, 0, 0, 0.3);
            border-radius: 12px;
            padding: 20px;
        }

        .platform-icon {
            font-size: 24px;
            color: var(--primary);
            margin-bottom: 10px;
        }

        .platform-name {
            font-size: 14px;
            font-weight: 700;
        }

        .generate-content-btn {
            padding: 16px 40px;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            border: none;
            border-radius: 12px;
            font-size: 18px;
            font-weight: 900;
            color: white;
            cursor: pointer;
            transition: all 0.3s;
            box-shadow: 0 8px 25px rgba(139, 92, 246, 0.5);
        }

        .generate-content-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 12px 30px rgba(139, 92, 246, 0.7);
        }

        /* Footer */
        .footer {
            background: rgba(0, 0, 0, 0.9);
            backdrop-filter: blur(10px);
            border-top: 1px solid rgba(139, 92, 246, 0.2);
            padding: 15px 0;
            position: fixed;
            bottom: 0;
            left: 0;
            right: 0;
            z-index: 80;
        }

        .footer-content {
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .footer-stats {
            display: flex;
            align-items: center;
            gap: 25px;
        }

        .footer-stat {
            display: flex;
            align-items: center;
            gap: 8px;
            font-size: 14px;
        }

        .footer-stat i {
            color: var(--primary);
        }

        .footer-stat-label {
            color: var(--gray);
        }

        .footer-stat-value {
            font-weight: 700;
        }

        .footer-time {
            color: var(--gray);
            font-size: 14px;
        }

        .time-value {
            color: white;
            font-weight: 700;
        }

        /* Animations */
        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.5; }
            100% { opacity: 1; }
        }

        /* Responsive */
        @media (max-width: 1024px) {
            .stats-grid {
                grid-template-columns: 1fr;
            }
            
            .affiliates-grid {
                grid-template-columns: 1fr;
            }
            
            .campaign-stats {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 768px) {
            .header-content {
                flex-direction: column;
                gap: 15px;
                align-items: flex-start;
            }
            
            .header-controls {
                width: 100%;
                justify-content: space-between;
            }
            
            .nav-content {
                padding: 10px 0;
            }
            
            .platforms-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            
            .footer-content {
                flex-direction: column;
                gap: 10px;
                align-items: flex-start;
            }
            
            .footer-stats {
                flex-wrap: wrap;
                gap: 15px;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="header">
        <div class="container">
            <div class="header-content">
                <div class="logo">
                    <div class="logo-icon">
                        <i class="fas fa-rocket"></i>
                    </div>
                    <div>
                        <div class="logo-text">THAI AFFILIATE EMPIRE</div>
                        <div class="logo-subtitle">Complete Automation System</div>
                    </div>
                </div>
                <div class="header-controls">
                    <div class="status-badge">
                        <div class="status-dot"></div>
                        <span>LIVE</span>
                    </div>
                    <button class="auto-mode-btn" id="autoModeBtn">
                        <i class="fas fa-play" id="autoModeIcon"></i>
                        AUTO MODE <span id="autoModeStatus">OFF</span>
                    </button>
                </div>
            </div>
        </div>
    </header>

    <!-- Navigation -->
    <nav class="nav">
        <div class="container">
            <div class="nav-content">
                <button class="nav-tab active" data-tab="dashboard">
                    <i class="fas fa-chart-bar"></i>
                    Dashboard
                </button>
                <button class="nav-tab" data-tab="products">
                    <i class="fas fa-shopping-bag"></i>
                    Product Scout
                </button>
                <button class="nav-tab" data-tab="affiliates">
                    <i class="fas fa-users"></i>
                    Affiliates
                </button>
                <button class="nav-tab" data-tab="campaigns">
                    <i class="fas fa-bullseye"></i>
                    Campaigns
                </button>
                <button class="nav-tab" data-tab="content">
                    <i class="fas fa-brain"></i>
                    AI Content
                </button>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="main-content">
        <div class="container">
            <!-- Dashboard Tab -->
            <div class="tab-content active" id="dashboard-tab">
                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-header">
                            <div class="stat-label">TODAY</div>
                            <div class="stat-icon icon-today">
                                <i class="fas fa-bolt"></i>
                            </div>
                        </div>
                        <div class="stat-value">฿28,450</div>
                        <div class="stat-trend">
                            <i class="fas fa-arrow-up"></i>
                            +15% vs yesterday
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-header">
                            <div class="stat-label">THIS WEEK</div>
                            <div class="stat-icon icon-week">
                                <i class="fas fa-chart-line"></i>
                            </div>
                        </div>
                        <div class="stat-value">฿156,780</div>
                        <div class="stat-trend">
                            <i class="fas fa-arrow-up"></i>
                            +20% vs last week
                        </div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-header">
                            <div class="stat-label">THIS MONTH</div>
                            <div class="stat-icon icon-month">
                                <i class="fas fa-dollar-sign"></i>
                            </div>
                        </div>
                        <div class="stat-value">฿567,890</div>
                        <div class="stat-trend">
                            <i class="fas fa-arrow-up"></i>
                            +25% vs last month
                        </div>
                    </div>
                </div>

                <div class="mini-stats">
                    <div class="mini-stat">
                        <div class="mini-stat-header">
                            <i class="fas fa-shopping-bag mini-stat-icon"></i>
                            <div class="mini-stat-label">Active Products</div>
                        </div>
                        <div class="mini-stat-value">4</div>
                    </div>
                    <div class="mini-stat">
                        <div class="mini-stat-header">
                            <i class="fas fa-users mini-stat-icon"></i>
                            <div class="mini-stat-label">Elite Affiliates</div>
                        </div>
                        <div class="mini-stat-value">2</div>
                    </div>
                    <div class="mini-stat">
                        <div class="mini-stat-header">
                            <i class="fas fa-bullseye mini-stat-icon"></i>
                            <div class="mini-stat-label">Live Campaigns</div>
                        </div>
                        <div class="mini-stat-value">2</div>
                    </div>
                    <div class="mini-stat">
                        <div class="mini-stat-header">
                            <i class="fas fa-chart-line mini-stat-icon"></i>
                            <div class="mini-stat-label">Avg ROI</div>
                        </div>
                        <div class="mini-stat-value">545%</div>
                    </div>
                </div>

                <div class="auto-pilot-card" id="autoPilotCard" style="display: none;">
                    <div class="auto-pilot-header">
                        <div class="auto-pilot-dot"></div>
                        <div class="auto-pilot-title">AUTO-PILOT ACTIVE</div>
                    </div>
                    <div class="auto-pilot-grid">
                        <div class="auto-pilot-item">
                            <div class="auto-pilot-item-header">
                                <i class="fas fa-search auto-pilot-item-icon"></i>
                                <div class="auto-pilot-item-label">Product Scanning</div>
                            </div>
                            <div class="auto-pilot-item-value">Every 5 min</div>
                        </div>
                        <div class="auto-pilot-item">
                            <div class="auto-pilot-item-header">
                                <i class="fas fa-robot auto-pilot-item-icon"></i>
                                <div class="auto-pilot-item-label">Content Generation</div>
                            </div>
                            <div class="auto-pilot-item-value">24/7 Active</div>
                        </div>
                        <div class="auto-pilot-item">
                            <div class="auto-pilot-item-header">
                                <i class="fas fa-bolt auto-pilot-item-icon"></i>
                                <div class="auto-pilot-item-label">Budget Optimization</div>
                            </div>
                            <div class="auto-pilot-item-value">Real-time</div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Products Tab -->
            <div class="tab-content" id="products-tab">
                <div class="scanner-card">
                    <div class="scanner-header">
                        <div>
                            <div class="scanner-title">AI Product Scout</div>
                            <div class="scanner-description">Auto-discover winning products from Thai e-commerce</div>
                        </div>
                        <button class="scan-btn" id="scanBtn">
                            <i class="fas fa-search"></i>
                            Start Scan
                        </button>
                    </div>
                    <div class="progress-container" id="progressContainer" style="display: none;">
                        <div class="progress-header">
                            <span>Scanning Shopee, Lazada, TikTok...</span>
                            <span id="progressPercent">0%</span>
                        </div>
                        <div class="progress-bar">
                            <div class="progress-fill" id="progressFill" style="width: 0%;"></div>
                        </div>
                    </div>
                </div>

                <div class="products-table">
                    <div class="table-header">
                        <div class="table-title">Product Portfolio</div>
                    </div>
                    <table>
                        <thead>
                            <tr>
                                <th>Product</th>
                                <th>Platform</th>
                                <th>Price</th>
                                <th>Commission</th>
                                <th>Trend</th>
                                <th>Sales</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody id="productsTableBody">
                            <!-- Products will be inserted here by JavaScript -->
                        </tbody>
                    </table>
                </div>
            </div>

            <!-- Affiliates Tab -->
            <div class="tab-content" id="affiliates-tab">
                <div class="affiliates-grid" id="affiliatesGrid">
                    <!-- Affiliates will be inserted here by JavaScript -->
                </div>
            </div>

            <!-- Campaigns Tab -->
            <div class="tab-content" id="campaigns-tab">
                <div class="campaigns-list" id="campaignsList">
                    <!-- Campaigns will be inserted here by JavaScript -->
                </div>
            </div>

            <!-- Content Tab -->
            <div class="tab-content" id="content-tab">
                <div class="content-generator">
                    <div class="content-icon">
                        <i class="fas fa-brain"></i>
                    </div>
                    <h2 class="content-title">AI Content Factory</h2>
                    <p class="content-description">
                        Generate unlimited Thai + English content for all platforms in seconds. 
                        TikTok scripts, Instagram captions, product reviews, SEO descriptions, and more.
                    </p>
                    
                    <div class="platforms-grid">
                        <div class="platform-item">
                            <div class="platform-icon">
                                <i class="fab fa-tiktok"></i>
                            </div>
                            <div class="platform-name">TikTok</div>
                        </div>
                        <div class="platform-item">
                            <div class="platform-icon">
                                <i class="fab fa-instagram"></i>
                            </div>
                            <div class="platform-name">Instagram</div>
                        </div>
                        <div class="platform-item">
                            <div class="platform-icon">
                                <i class="fab fa-line"></i>
                            </div>
                            <div class="platform-name">LINE OA</div>
                        </div>
                        <div class="platform-item">
                            <div class="platform-icon">
                                <i class="fas fa-shopping-bag"></i>
                            </div>
                            <div class="platform-name">Shopee</div>
                        </div>
                    </div>
                    
                    <button class="generate-content-btn" id="generateContentBtn">
                        Generate Content Now
                    </button>
                </div>
            </div>
        </div>
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="container">
            <div class="footer-content">
                <div class="footer-stats">
                    <div class="footer-stat">
                        <i class="fas fa-database"></i>
                        <span class="footer-stat-label">System Status:</span>
                        <span class="footer-stat-value">Optimal</span>
                    </div>
                    <div class="footer-stat">
                        <i class="fas fa-globe"></i>
                        <span class="footer-stat-label">Market:</span>
                        <span class="footer-stat-value">Thailand</span>
                    </div>
                </div>
                <div class="footer-time">
                    Last Updated: <span class="time-value" id="currentTime">Loading...</span>
                </div>
            </div>
        </div>
    </footer>

    <script>
        // Demo data
        const products = [
            { id: 1, name: 'Wireless Earbuds Pro', platform: 'Shopee', price: 890, commission: 12, trend: 87, demand: 'High', status: 'active', sales: 234, margin: 28 },
            { id: 2, name: 'Smart Watch Fitness', platform: 'Lazada', price: 1290, commission: 15, trend: 92, demand: 'Very High', status: 'active', sales: 456, margin: 32 },
            { id: 3, name: 'Phone Holder Mount', platform: 'TikTok', price: 290, commission: 8, trend: 78, demand: 'Medium', status: 'active', sales: 789, margin: 45 },
            { id: 4, name: 'LED Strip Lights', platform: 'Shopee', price: 590, commission: 10, trend: 95, demand: 'Very High', status: 'active', sales: 567, margin: 38 },
            { id: 5, name: 'Gaming Mouse RGB', platform: 'Lazada', price: 790, commission: 13, trend: 73, demand: 'Medium', status: 'paused', sales: 123, margin: 25 }
        ];

        const affiliates = [
            { id: 1, name: 'TechReview_TH', followers: '45K', platform: 'TikTok', conversion: 8.5, earnings: 12450, status: 'elite' },
            { id: 2, name: 'GadgetGuru_BKK', followers: '32K', platform: 'Instagram', conversion: 6.2, earnings: 8900, status: 'active' },
            { id: 3, name: 'ThaiShopPro', followers: '78K', platform: 'Shopee', conversion: 9.1, earnings: 18600, status: 'elite' },
            { id: 4, name: 'BangkokDeals', followers: '23K', platform: 'LINE', conversion: 5.8, earnings: 6700, status: 'active' }
        ];

        const campaigns = [
            { id: 1, product: 'Wireless Earbuds Pro', channel: 'TikTok', spend: 2500, revenue: 18900, roi: 656, status: 'winning' },
            { id: 2, product: 'Smart Watch Fitness', channel: 'Instagram', spend: 3200, revenue: 24500, roi: 666, status: 'winning' },
            { id: 3, product: 'Phone Holder Mount', channel: 'Shopee', spend: 1800, revenue: 8900, roi: 394, status: 'testing' }
        ];

        // DOM Elements
        const autoModeBtn = document.getElementById('autoModeBtn');
        const autoModeIcon = document.getElementById('autoModeIcon');
        const autoModeStatus = document.getElementById('autoModeStatus');
        const autoPilotCard = document.getElementById('autoPilotCard');
        const navTabs = document.querySelectorAll('.nav-tab');
        const tabContents = document.querySelectorAll('.tab-content');
        const scanBtn = document.getElementById('scanBtn');
        const progressContainer = document.getElementById('progressContainer');
        const progressFill = document.getElementById('progressFill');
        const progressPercent = document.getElementById('progressPercent');
        const productsTableBody = document.getElementById('productsTableBody');
        const affiliatesGrid = document.getElementById('affiliatesGrid');
        const campaignsList = document.getElementById('campaignsList');
        const generateContentBtn = document.getElementById('generateContentBtn');
        const currentTime = document.getElementById('currentTime');

        // State
        let autoMode = false;
        let isScanning = false;

        // Initialize the dashboard
        function initDashboard() {
            renderProducts();
            renderAffiliates();
            renderCampaigns();
            updateTime();
            
            // Update time every second
            setInterval(updateTime, 1000);
        }

        // Render products table
        function renderProducts() {
            productsTableBody.innerHTML = '';
            
            products.forEach(product => {
                const row = document.createElement('tr');
                row.innerHTML = `
                    <td>
                        <div style="font-weight: 700;">${product.name}</div>
                        <div style="font-size: 12px; color: #64748b;">Margin: ${product.margin}%</div>
                    </td>
                    <td>
                        <span class="platform-badge">${product.platform}</span>
                    </td>
                    <td style="font-weight: 700;">฿${product.price}</td>
                    <td style="color: #10b981; font-weight: 700;">${product.commission}%</td>
                    <td>
                        <div class="trend-bar">
                            <div class="bar-container">
                                <div class="bar-fill" style="width: ${product.trend}%;"></div>
                            </div>
                            <span style="font-weight: 700;">${product.trend}</span>
                        </div>
                    </td>
                    <td style="font-weight: 700;">${product.sales}</td>
                    <td>
                        <button class="generate-btn" onclick="generateContent(${product.id})">
                            <i class="fas fa-robot"></i>
                            Generate
                        </button>
                    </td>
                `;
                productsTableBody.appendChild(row);
            });
        }

        // Render affiliates grid
        function renderAffiliates() {
            affiliatesGrid.innerHTML = '';
            
            affiliates.forEach(affiliate => {
                const card = document.createElement('div');
                card.className = 'affiliate-card';
                card.innerHTML = `
                    <div class="affiliate-header">
                        <div>
                            <div class="affiliate-name">${affiliate.name}</div>
                            <div class="affiliate-meta">
                                <span class="affiliate-platform">${affiliate.platform}</span>
                                <span class="affiliate-followers">${affiliate.followers} followers</span>
                            </div>
                        </div>
                        ${affiliate.status === 'elite' ? 
                            '<div class="elite-badge">ELITE</div>' : ''}
                    </div>
                    
                    <div class="affiliate-stats">
                        <div class="affiliate-stat">
                            <div class="affiliate-stat-label">Conversion Rate</div>
                            <div class="affiliate-stat-value conversion-value">${affiliate.conversion}%</div>
                        </div>
                        <div class="affiliate-stat">
                            <div class="affiliate-stat-label">Total Earnings</div>
                            <div class="affiliate-stat-value">฿${affiliate.earnings.toLocaleString()}</div>
                        </div>
                    </div>
                    
                    <button class="affiliate-action">
                        View Performance
                    </button>
                `;
                affiliatesGrid.appendChild(card);
            });
        }

        // Render campaigns list
        function renderCampaigns() {
            campaignsList.innerHTML = '';
            
            campaigns.forEach(campaign => {
                const card = document.createElement('div');
                card.className = 'campaign-card';
                card.innerHTML = `
                    <div class="campaign-header">
                        <div class="campaign-info">
                            <h3>${campaign.product}</h3>
                            <span class="campaign-channel">${campaign.channel}</span>
                        </div>
                        ${campaign.status === 'winning' ? 
                            '<div class="winning-badge"><i class="fas fa-chart-line"></i> WINNING</div>' : ''}
                    </div>
                    
                    <div class="campaign-stats">
                        <div class="campaign-stat">
                            <div class="campaign-stat-label">Ad Spend</div>
                            <div class="campaign-stat-value spend-value">฿${campaign.spend.toLocaleString()}</div>
                        </div>
                        <div class="campaign-stat">
                            <div class="campaign-stat-label">Revenue</div>
                            <div class="campaign-stat-value revenue-value">฿${campaign.revenue.toLocaleString()}</div>
                        </div>
                        <div class="campaign-stat">
                            <div class="campaign-stat-label">ROI</div>
                            <div class="campaign-stat-value roi-value">${campaign.roi}%</div>
                        </div>
                    </div>
                `;
                campaignsList.appendChild(card);
            });
        }

        // Update current time
        function updateTime() {
            const now = new Date();
            currentTime.textContent = now.toLocaleTimeString('th-TH');
        }

        // Generate content function
        function generateContent(productId) {
            alert(`🤖 AI กำลังสร้างคอนเทนต์ภาษาไทย + อังกฤษ\n\n✅ TikTok Script\n✅ Instagram Caption\n✅ Product Review\n✅ SEO Description\n\nใช้เวลา 3 วินาที!`);
        }

        // Event Listeners
        autoModeBtn.addEventListener('click', () => {
            autoMode = !autoMode;
            
            if (autoMode) {
                autoModeIcon.className = 'fas fa-pause';
                autoModeStatus.textContent = 'ON';
                autoPilotCard.style.display = 'block';
            } else {
                autoModeIcon.className = 'fas fa-play';
                autoModeStatus.textContent = 'OFF';
                autoPilotCard.style.display = 'none';
            }
        });

        navTabs.forEach(tab => {
            tab.addEventListener('click', () => {
                // Remove active class from all tabs
                navTabs.forEach(t => t.classList.remove('active'));
                tabContents.forEach(t => t.classList.remove('active'));
                
                // Add active class to clicked tab
                tab.classList.add('active');
                document.getElementById(`${tab.dataset.tab}-tab`).classList.add('active');
            });
        });

        scanBtn.addEventListener('click', () => {
            if (isScanning) return;
            
            isScanning = true;
            scanBtn.disabled = true;
            scanBtn.innerHTML = '<i class="fas fa-sync-alt fa-spin"></i> Scanning...';
            progressContainer.style.display = 'block';
            
            let progress = 0;
            const interval = setInterval(() => {
                progress += 10;
                progressFill.style.width = `${progress}%`;
                progressPercent.textContent = `${progress}%`;
                
                if (progress >= 100) {
                    clearInterval(interval);
                    isScanning = false;
                    scanBtn.disabled = false;
                    scanBtn.innerHTML = '<i class="fas fa-search"></i> Start Scan';
                    
                    // Add a new product
                    const newProducts = [
                        { name: 'Bluetooth Speaker Mini', platform: 'Shopee', price: 690, commission: 11, trend: 88, demand: 'High' },
                        { name: 'USB-C Cable 3m', platform: 'Lazada', price: 190, commission: 7, trend: 82, demand: 'High' },
                        { name: 'Phone Case Silicone', platform: 'TikTok', price: 150, commission: 6, trend: 76, demand: 'Medium' }
                    ];
                    
                    const randomProduct = newProducts[Math.floor(Math.random() * newProducts.length)];
                    products.unshift({
                        id: products.length + 1,
                        ...randomProduct,
                        status: 'new',
                        sales: 0,
                        margin: Math.floor(Math.random() * 20) + 25
                    });
                    
                    renderProducts();
                    
                    // Hide progress after a delay
                    setTimeout(() => {
                        progressContainer.style.display = 'none';
                        progressFill.style.width = '0%';
                        progressPercent.textContent = '0%';
                    }, 2000);
                }
            }, 300);
        });

        generateContentBtn.addEventListener('click', () => {
            alert('🚀 AI Content Factory Activated!\n\nGenerating multi-platform content in Thai and English...\n\n• TikTok Video Scripts\n• Instagram Carousel Posts\n• LINE OA Broadcast Messages\n• Shopee Product Descriptions\n\nAll content will be ready in 5 seconds!');
        });

        // Initialize the dashboard when page loads
        document.addEventListener('DOMContentLoaded', initDashboard);
    </script>
</body>
</html>